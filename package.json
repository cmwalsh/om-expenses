{
  "name": "om-expense",
  "workspaces": [
    "./backend",
    "./common",
    "./frontend"
  ],
  "scripts": {
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm run dev",
    "common:dev": "cd common && npm run dev",
    "dev": "npm-run-all --parallel backend:dev frontend:dev common:dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}